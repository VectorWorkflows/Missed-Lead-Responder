# Patched versions. The old pins carried two known DoS vulnerabilities that
# were directly reachable, because every Twilio webhook parses multipart form
# data: python-multipart <0.0.7 and starlette <0.36.2 (pulled by fastapi 0.104).

fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20
pydantic-settings==2.7.1

motor==3.6.0
pymongo==4.9.2

twilio==9.4.1
python-telegram-bot==21.10
httpx==0.28.1

gspread==6.1.4
google-auth==2.37.0

APScheduler==3.11.0
tzdata==2024.2
